spring:
  datasource:
    url: jdbc:mysql://database-1.c00tuatqzua4.ap-northeast-2.rds.amazonaws.com:3306/myapp
    username: root
    password: docker1540
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
      show_sql: true
      format_sql: true

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace